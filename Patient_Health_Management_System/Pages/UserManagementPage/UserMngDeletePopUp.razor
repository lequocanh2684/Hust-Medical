@inject IUserService UserService

<MudDialog>
    <DialogContent>
        <MudText>Bạn có chắc muốn xoá người dùng này không?</MudText>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel">Huỷ</MudButton>
        <MudButton Color="Color.Error" Variant="Variant.Filled" OnClick="Submit">Xoá</MudButton>
    </DialogActions>
</MudDialog>

@code {

    [CascadingParameter] MudDialogInstance MudDialog { get; set; }

    [Parameter]
    public string Id { get; set; }

    [Parameter]
    public string UserName { get; set; }

    [Parameter]
    public string UserId { get; set; }

    private void Cancel()
    {
        MudDialog.Cancel();
    }

    private async Task Submit()
    {
        await UserService.DeleteUserByUserId(Id);
        MudDialog.Close(DialogResult.Ok(true));
    }
}